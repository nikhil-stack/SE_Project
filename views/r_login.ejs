<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../css/r_login.css">

    <title>Document</title>
</head>

<body>
    <div class="main__body">
        <div class="main__body__right">
            <div class="form">

                <form action="/restaurants/login" method="POST">
                    <input type="email" required placeholder="username" name="email" id="email_inp">
                    <input type="password" required placeholder="password" name="password" id="password_inp">

                    <button type="submit">Login</button>
                </form>
                <div class="form__footer">
                    <a id="forgot_pass" href="#">Forgot Password?</a>
                    <a id="create_an_acc" href="/restaurants/signup">Create an account</a>
                </div>
            </div>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@9"></script>
    <script>
        Swal.fire({
            title: '<strong>Confirmation</strong>',
            icon: 'info',
            html: 'This page is for restaurant owners to login. ' +

                '<p> Please confirm your identity</p>',
            showCloseButton: true,
            showCancelButton: true,
            focusConfirm: false,
            confirmButtonText: '<i class="fa fa-thumbs-up"></i> Restaurant Owner!',
            confirmButtonAriaLabel: '',
            cancelButtonText: ' <a href="./login_user.html"><i class="fa fa-thumbs-down"></i>User</a>',
            cancelButtonAriaLabel: ' <a href="./login_user.html"></i>User</a>',

        });


        var forgotpass = document.getElementById('forgot_pass');
        forgotpass.addEventListener('click', forgotPassPropmt);

        function forgotPassPropmt() { //forgot pass function 
            var email = document.getElementById('email_inp').value;
            console.log(email);
            // will check if email is in our database if not then wil prompt that email not registered
            // if email in database then
            if (email == "") {
                Swal.fire(
                    'Please enter your email!'

                )
            } else {
                Swal.fire(
                    'Reset link generated!',
                    'Please check your registered email!',
                    'success'
                )
            }
        }



        function validate() {
            console.log("submit");
            var pass1 = document.forms['loginform']['email'].value;
            var pass2 = document.forms['loginform']['pass'].value;
            // to check in db



        }
    </script>
</body>

</html>